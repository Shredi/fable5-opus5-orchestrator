# Profile switch → OPUS-PRIMARY chair

Opus holds the chair by choice. Effective now: opus is the
ceiling and verifies all but high-stakes closes; fable is capped (≤2
spawns/task without the user's OK) to PLANNER for hard, irreversible
or multi-system plans (plan to scratch before the checkpoint) and
high-stakes VERIFIER (production relays/edge proxies/actuators,
irreversible migrations, plugin/fork releases, large refactors). Fable
limit spent → the role falls to opus, noted in the ledger. Security
stays on opus. Every other rule from the already-injected core
profile stays in force.
